spring.application.name=citizenportal
# This file is intentionally left mostly empty as we use YAML configuration
# It's kept for compatibility with some tools that might expect it
spring.profiles.active=dev